
# --- Suffix Aliases ---

# Text files
alias -s err=vim
alias -s log=vim
alias -s output=vim
alias -s ts=vim
alias -s txt=vim

# Non-executable scripting files
alias -s pm=vim
alias -s vim=vim

# Data serialization files
alias -s cfg=vim
alias -s conf=vim
alias -s confg=vim
alias -s config=vim
alias -s html=vim
alias -s json=vim
alias -s xml=vim
alias -s yaml=vim
alias -s yml=vim

# C files
alias -s c=vim
alias -s h=vim

# Verilog files
alias -s v=vim
alias -s vh=vim
alias -s sv=vim
alias -s svh=vim

