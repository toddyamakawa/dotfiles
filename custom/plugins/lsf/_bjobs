#compdef bjobs

_bjobs() {
	local curcontext="$curcontext" state line
	typeset -A opt_args
	format="id:8 stat:5 queue:10 proj:15 submit_time:13 start_time"
	jobs=$(bjobs -noheader -o "$format" | sed "s/^\([0-9]\+\)\s\+\(.*\)/\1':\2'/")
	_arguments "*:jobid:(($jobs))"
}

_bjobs "$@"

